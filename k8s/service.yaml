apiVersion: v1
kind: Service
metadata:
  name: cloud-sentinel
  namespace: cloud-sentinel
spec:
  selector:
    app: cloud-sentinel
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
  type: ClusterIP
